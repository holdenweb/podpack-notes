This text ships inside the notes app and was copied to the host the first time
the app was installed. Edit it on the host and reload the page: the change shows
up without a rebuild, because the running app reads the host copy, not the one
packaged in the image.
